<%= form_with(model: chair) do |form| %>
  <% if chair.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chair.errors.count, "error") %> prohibited this chair from being saved:</h2>

      <ul>
        <% chair.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :char_name %>
    <%= form.text_field :char_name %>
  </div>

  <div class="field">
    <%= form.label :salon_id %>
    <%= form.text_field :salon_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
